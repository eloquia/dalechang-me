import React from 'react';

const CurrentStack = () => {
  // const stackQuery = useStaticQuery(graphql`
  //   query stackQuery {
  //     stackItemsJson {
  //       name
  //       children {
  //         id
  //       }
  //     }
  //   }
  // `);
  return (
    <section>
      <h1>Current Stack</h1>
    </section>
  );
};

export default CurrentStack;
